import React from 'react';
import { hot } from 'react-hot-loader';

function App (props) {
    return (
        <div className='none'>
            Hello World
        </div>
    )
}

export default hot(module)(App);
